# Large CC0 / public-domain assets are fetched by tools/fetch_assets.py and
# tools/make_sky_hdri.py, not committed.
assets/textures/
assets/hdri/
assets/starmap/
assets/geo/
!assets/plan/

# Scratch renders, previews and inspection sheets
out/
renders/*_sheet.png
# the measured type boxes annotate.py hands to check_figure.py: derived from
# the figure on every run, and regenerated by render.sh --both
renders/*_blocks.json
*.blend1
__pycache__/

# Movie frame sequences are build intermediates: a 15 s 1920x1280 pass is 450
# PNGs and hundreds of MB, and render.sh encodes them to an .mp4 beside the
# directory. The .mp4 is the deliverable and stays tracked.
renders/movie_*/
